输入：
1、将全部png文件、list.txt文件放在可执行程序相同目录下；
2、可执行程序运行时不需要输入参数；
3、程序读取list.txt，循环读取list.txt中每行记录，作为文件名读取同目录下对应png文件

输出：
1、处理读入的每个png文件，将处理后的结果输出到与png文件同名同目录下的txt文件中，一个png文件对应一个txt
2、每个txt中的输出结果按照第一次发布“大作业2”时的要求，下面重述一下格式：
“输出格式：第一行：键盘行数N（空白间隔也算一行）、各行高度H、各行元素个数M；后续N行：从左往右依次为各个元素的宽度、种类（以WGB分别表示白健、灰键、蓝键，种类S表示键位之间的间隔区域）；”
